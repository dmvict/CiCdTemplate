
name : wrong_branch_master

on :
  pull_request_target :
    branches :
      - beta

jobs :

  wrong_branch :
    uses : Wandalen/RustModuleCd/.github/workflows/WrongBranch.yml@master
    with :
      src_branch : 'alpha'
      dst_branch : '${{ github.base_ref }}'
    secrets :
      PRIVATE_GITHUB_BOT_TOKEN : '${{ secrets.PRIVATE_GITHUB_BOT_TOKEN }}'
