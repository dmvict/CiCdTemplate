name : module2

on : push

env :
  CARGO_TERM_COLOR : always

jobs :

  # module2

  # test :
  #   uses : dmvict/CiCdTemplate/.github/workflows/StandardRustPush.yml@workflow
  #   with :
  #     manifest_path : 'module/module2/Cargo.toml'
  #     module_name : 'module2'
  #     commit_message : ${{ github.event.head_commit.message }}

  success :
    if : |
      contains( github.event.head_commit.message, 'module2' )
      || contains( github.event.head_commit.message, '!test' )
    runs-on : ubuntu-latest
    steps :
      - run : exit 0
